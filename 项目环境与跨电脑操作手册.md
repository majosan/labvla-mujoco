# 项目环境与跨电脑操作手册

适用项目：`/home/josan/projects/labvla-mujoco`

当前确认可用的主环境：`labvla-cu124`

当前确认可用的解释器路径：

```bash
/home/josan/miniforge3/envs/labvla-cu124/bin/python
```

当前这套环境里的最终核心版本：

```text
torch 2.7.1+cu126
torchvision 0.22.1+cu126
torchaudio 2.7.1+cu126
bitsandbytes 0.49.2
flash_attn 2.8.3
```

---

## 1. 日常重新进入项目时怎么操作

每次新开一个终端，按这个顺序：

```bash
source ~/miniforge3/bin/activate
conda activate labvla-cu124
cd ~/projects/labvla-mujoco
```

然后确认当前 Python 没进错环境：

```bash
which python
python -V
python -c "import torch; print(torch.__version__); print(torch.cuda.is_available()); print(torch.version.cuda)"
```

预期应接近：

```text
/home/josan/miniforge3/envs/labvla-cu124/bin/python
Python 3.10.x
2.7.1+cu126
True
12.6
```

如果 `which python` 不是上面的 conda 路径，说明你还没有切进正确环境。

---

## 2. VS Code 里怎么切换解释器

这个项目后续开发和运行，建议始终把解释器切到：

```text
/home/josan/miniforge3/envs/labvla-cu124/bin/python
```

常用方法：

1. `Ctrl+Shift+P`
2. 搜索 `Python: Select Interpreter`
3. 选择上面的 conda 解释器路径

如果 VS Code 还在扫旧 `.venv`，看到类似下面这种报错：

```text
/home/josan/projects/labvla-mujoco/.venv/bin/python: No module named pip
```

这通常不是当前 conda 环境坏了，而是编辑器还指着旧 `.venv`。

---

## 3. 当前环境的已知正确状态

本机已经验证通过：

```bash
/home/josan/miniforge3/envs/labvla-cu124/bin/python -c "import torch; print('torch_version', torch.__version__); print('torch_cuda', torch.version.cuda); print('cuda_available', torch.cuda.is_available()); print('device', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'no gpu')"
```

验证结果：

```text
torch_version 2.7.1+cu126
torch_cuda 12.6
cuda_available True
device NVIDIA GeForce RTX 4060
```

这说明当前最终可用方案是：

- conda 负责创建 Python 环境
- pip 安装官方 CUDA wheel 版 PyTorch
- `LabVLA` 其余依赖再补齐到项目要求版本

---

## 4. 当前推荐安装方式

### 4.1 创建环境

```bash
source ~/miniforge3/bin/activate
conda create -n labvla-cu124 python=3.10 -y
conda activate labvla-cu124
```

### 4.2 安装项目主线 PyTorch

当前更推荐直接对齐 `LabVLA` 自己的主线版本：

```bash
python -m pip install torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1 --index-url https://download.pytorch.org/whl/cu126
```

### 4.3 安装 `flash_attn` 前置

```bash
python -m pip install psutil packaging ninja wheel setuptools
```

### 4.4 安装 `flash_attn`

```bash
python -m pip install flash_attn==2.8.3 --no-build-isolation
```

### 4.5 安装 `bitsandbytes`

```bash
python -m pip install bitsandbytes
```

### 4.6 安装其余 LabVLA 依赖

注意：`LabVLA/requirements.txt` 里有一个当前不可用的版本 `pyyaml-include==1.4.0`，在新机器上安装时需要改成 `1.4.1`。

最稳做法：

```bash
grep -Ev '^(--extra-index-url)' ~/projects/labvla-mujoco/LabVLA/requirements.txt > /tmp/labvla-requirements.txt
sed -i 's/pyyaml-include==1.4.0/pyyaml-include==1.4.1/' /tmp/labvla-requirements.txt
python -m pip install -r /tmp/labvla-requirements.txt
```

### 4.7 验证 CUDA 和关键依赖

```bash
python - <<'PY'
import importlib, torch
mods = ['torch', 'torchvision', 'torchaudio', 'transformers', 'accelerate', 'deepspeed', 'bitsandbytes', 'flash_attn']
for name in mods:
    m = importlib.import_module(name)
    print(name, getattr(m, '__version__', 'no-version'))
print('cuda_available', torch.cuda.is_available())
print('cuda_version', torch.version.cuda)
print('device', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'no gpu')
PY
```

---

## 5. 为什么不再优先用旧 conda 安装命令

之前试过：

```bash
conda install pytorch torchvision pytorch-cuda=12.4 -c pytorch -c nvidia --strict-channel-priority -y
```

在这台机器上实际踩到两个问题：

1. 下载大包时容易出现 `IncompleteRead(...)`
2. 环境里会出现 `pytorch-cuda` 在，但 `pytorch/torchvision` 被解成 conda-forge 的 CPU 版

所以当前项目里，稳定路线改成了：

- `conda create` 创建环境
- `pip --index-url https://download.pytorch.org/whl/cu124` 安装 GPU 版 torch

---

## 6. 常用操作命令速查

### 6.1 进入项目

```bash
source ~/miniforge3/bin/activate
conda activate labvla-cu124
cd ~/projects/labvla-mujoco
```

如果你在多环境之间切换频繁，不要假设当前 shell 的 `python` 是对的。运行任何下载、推理、服务命令前，先确认：

```bash
which python
python -c "import sys; print(sys.executable)"
```

### 6.2 检查 GPU

```bash
nvidia-smi
```

### 6.3 检查当前 Python 和 torch

```bash
which python
python -c "import torch; print(torch.__file__); print(torch.__version__); print(torch.version.cuda); print(torch.cuda.is_available())"
```

### 6.4 安装 bitsandbytes

```bash
python -m pip install bitsandbytes
python -c "import bitsandbytes as bnb; print(bnb.__version__)"
```

### 6.5 安装 flash_attn

```bash
python -m pip install psutil packaging ninja wheel setuptools
python -m pip install flash_attn==2.8.3 --no-build-isolation
python -c "import flash_attn; print(flash_attn.__version__)"
```

### 6.6 安装项目依赖

```bash
grep -Ev '^(--extra-index-url)' ~/projects/labvla-mujoco/LabVLA/requirements.txt > /tmp/labvla-requirements.txt
sed -i 's/pyyaml-include==1.4.0/pyyaml-include==1.4.1/' /tmp/labvla-requirements.txt
python -m pip install -r /tmp/labvla-requirements.txt
```

### 6.7 下载模型

优先使用 Python 入口，避免本机没有 `huggingface-cli` 可执行文件时下载失败。国内网络优先使用 Hugging Face 镜像：

```bash
cd ~/projects/labvla-mujoco
HF_ENDPOINT=https://hf-mirror.com python - <<'PY'
from huggingface_hub import snapshot_download
snapshot_download(
    repo_id='zjunlp/LabVLA-5B-Base',
    local_dir='./LabVLA-5B-Base',
    resume_download=True,
    local_dir_use_symlinks=False,
)
PY
```

如果本机已经安装了 `huggingface-cli`，也可以用：

```bash
HF_ENDPOINT=https://hf-mirror.com huggingface-cli download zjunlp/LabVLA-5B-Base --local-dir ./LabVLA-5B-Base --resume-download
```

### 6.7.1 schema 本地化管理

当前项目不再依赖运行时再去 Hugging Face Space 临时找 schema。我们已经把可用的 schema 模板正式纳入项目：

```text
~/projects/labvla-mujoco/scripts/labvla_schema.json
```

用途：

- 这是当前本机跑通 `LabVLA-5B-Base` 所使用的 schema 基线
- 后续如果你要适配自定义状态或动作格式，就以这个文件为蓝本修改
- `scripts/infer_quantized.py` 和 `scripts/test_client.py` 会在模型目录缺少 `labvla_schema.json` 时，自动从这里复制一份到 `LabVLA-5B-Base/`

所以另一台电脑上，只要项目目录完整复制过去，通常不需要再单独手工补 schema 文件

### 6.8 本地量化推理验证

```bash
cd ~/projects/labvla-mujoco
PYTHONPATH="~/projects/labvla-mujoco/LabVLA" /home/josan/miniforge3/envs/labvla-cu124/bin/python scripts/infer_quantized.py --pretrained_path ./LabVLA-5B-Base --vlm_path Qwen/Qwen3-VL-4B-Instruct --device cuda
```

当前已验证结果的关键特征：

- 首次加载大约 `208s`
- 单次推理大约 `10.7s`
- 按 `nvidia-smi` 复测的峰值显存大约 `7903 MB`
- 输出 action 为 `50 x 8` 的 `float32`

### 6.9 启动 WebSocket 服务

```bash
cd ~/projects/labvla-mujoco
PYTHONPATH="~/projects/labvla-mujoco/LabVLA" /home/josan/miniforge3/envs/labvla-cu124/bin/python LabVLA/deployment/serve_labvla.py --pretrained_path ./LabVLA-5B-Base --vlm_path Qwen/Qwen3-VL-4B-Instruct --device cuda --port 8000
```

注意：`LabVLA-5B-Base/` 默认不包含 `labvla_schema.json`，本机实际运行时需要从 Hugging Face Space `zjunlp/lab-vla` 补一份同名 schema 到模型目录。

### 6.10 运行 WebSocket 测试客户端

```bash
cd ~/projects/labvla-mujoco
/home/josan/miniforge3/envs/labvla-cu124/bin/python scripts/test_client.py --host 127.0.0.1 --port 8000
```

当前已验证结果的关键特征：

- 首帧返回 metadata，不是 action
- observation 需要按 msgpack ndarray 编码发送
- 一次往返 RTT 大约 `2231 ms`
- 服务端 `infer_ms` 大约 `2228 ms`
- 输出 action 为 `50 x 8` 的 `float32`

```bash
cd ~/projects/labvla-mujoco
python scripts/serve_labvla_4bit.py --pretrained_path ./LabVLA-5B-Base --vlm_path Qwen/Qwen3-VL-4B-Instruct --port 8000 --device cuda
```

---

## 7. 另一台电脑怎么重新搭建

建议按下面顺序重建，不要直接拷整个 conda 环境目录过去复用。

### 7.1 先准备基础条件

另一台机器至少要满足：

- NVIDIA 驱动正常
- 能在终端运行 `nvidia-smi`
- WSL2 或 Linux 可正常使用 GPU
- 可以访问 GitHub / Hugging Face / PyTorch wheel 源 / PyPI

### 7.2 安装 Miniforge

```bash
curl -L https://github.com/conda-forge/miniforge/releases/latest/download/Miniforge3-Linux-x86_64.sh -o ~/Miniforge3.sh
bash ~/Miniforge3.sh
source ~/miniforge3/bin/activate
```

### 7.3 准备项目目录

方式一：复制整个项目目录

方式二：重新拉代码后，再把本地脚本和模型目录同步过去

至少需要这些内容：

- `labvla-mujoco/`
- `labvla-mujoco/LabVLA/`
- `labvla-mujoco/scripts/`
- `labvla-mujoco/项目环境与跨电脑操作手册.md`

### 7.4 在另一台电脑完整复现当前已验证环境

按下面顺序执行：

```bash
source ~/miniforge3/bin/activate
conda create -n labvla-cu124 python=3.10 -y
conda activate labvla-cu124

python -m pip install torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1 --index-url https://download.pytorch.org/whl/cu126
python -m pip install psutil packaging ninja wheel setuptools
python -m pip install flash_attn==2.8.3 --no-build-isolation
python -m pip install bitsandbytes
python -m pip install websocket-client

grep -Ev '^(--extra-index-url)' ~/projects/labvla-mujoco/LabVLA/requirements.txt > /tmp/labvla-requirements.txt
sed -i 's/pyyaml-include==1.4.0/pyyaml-include==1.4.1/' /tmp/labvla-requirements.txt
python -m pip install -r /tmp/labvla-requirements.txt

cd ~/projects/labvla-mujoco
HF_ENDPOINT=https://hf-mirror.com python - <<'PY'
from huggingface_hub import snapshot_download
snapshot_download(
    repo_id='zjunlp/LabVLA-5B-Base',
    local_dir='./LabVLA-5B-Base',
    resume_download=True,
    local_dir_use_symlinks=False,
)
PY
```

如果你把当前这台电脑的整个项目目录直接打包复制过去，而且其中已经包含：

```text
labvla-mujoco/
├── LabVLA-5B-Base/
├── scripts/labvla_schema.json
├── scripts/infer_quantized.py
├── scripts/test_client.py
├── PHASE1-REPORT.md
└── 项目环境与跨电脑操作手册.md
```

那么另一台电脑通常不需要重新单独下载 schema，也不需要重新写 Phase 1 脚本。重点只剩：

- 重建 conda 环境
- 安装依赖
- 如模型目录没带过去，再下载模型

### 7.5 最终验证

```bash
python - <<'PY'
import importlib, torch
mods = ['torch', 'torchvision', 'torchaudio', 'transformers', 'accelerate', 'deepspeed', 'bitsandbytes', 'flash_attn']
for name in mods:
    m = importlib.import_module(name)
    print(name, getattr(m, '__version__', 'no-version'))
print('cuda_available', torch.cuda.is_available())
print('cuda_version', torch.version.cuda)
print('device', torch.cuda.get_device_name(0) if torch.cuda.is_available() else 'no gpu')
PY
```

预期核心结果应接近：

```text
torch 2.7.1+cu126
torchvision 0.22.1+cu126
torchaudio 2.7.1+cu126
bitsandbytes 0.49.2
flash_attn 2.8.3
cuda_available True
cuda_version 12.6
```

### 7.6 如果你想少下一些大文件

最值得直接拷贝的是：

```text
/home/josan/projects/labvla-mujoco/LabVLA-5B-Base
```

可选再拷：

```text
~/.cache/pip
```

前提是两台机器的系统、Python 版本、CPU 架构都一致。

结论上，最稳的迁移方式仍然是：

- 项目代码直接拷贝
- 模型目录直接拷贝
- conda 环境在新机器重建

---

## 8. 另一台电脑如何少下载一些东西

如果另一台电脑网络慢，优先考虑复制这两类大文件：

### 8.1 模型目录

```text
/home/josan/projects/labvla-mujoco/LabVLA-5B-Base
```

这是最值得直接拷贝的内容。

### 8.2 可选：pip 缓存

如果两台电脑系统、Python 版本、架构都一致，可以考虑复制 pip 缓存目录，减少 CUDA wheel 重下。

常见位置：

```text
~/.cache/pip
```

但相比之下，最稳的仍然是：

- 模型目录直接拷贝
- Python 环境在新机器重新创建

---

## 9. 不建议的做法

### 9.1 不建议直接打包整个 conda 环境目录跨机器复用

例如不建议直接拷：

```text
~/miniforge3/envs/labvla-cu124
```

原因：

- 路径可能绑定
- 动态库和驱动依赖容易不一致
- 另一台机器的系统库版本可能不同

### 9.2 不建议混用多个 Python 环境跑同一份项目

比如：

- 一会儿用 `.venv`
- 一会儿用 `chem-lab`
- 一会儿用 `labvla-cu124`

这样最容易出现“明明装了但 import 失败”或者“torch 是 CPU 版”的假象。

---

## 10. 迁移到另一台机器时建议打包的内容

建议打包这些：

```text
labvla-mujoco/
├── LabVLA/
├── scripts/
├── LabVLA-5B-Base/        # 如果磁盘允许，优先一起带走
└── 项目环境与跨电脑操作手册.md
```

如果只带方法，不带模型，另一台机器按本文档重新下载模型即可。

---

## 11. 以后新增操作时怎么维护这份文档

后续每次遇到下面这些内容，都建议追加到这份文档：

- 新的依赖安装命令
- 某个包的固定版本
- 某个命令的已知坑
- 另一台机器上验证通过的新步骤
- 下载加速、断点续传、镜像源经验
- 服务启动、客户端测试、推理脚本运行方式
- 像 `huggingface-cli` 缺失这类工具入口差异，以及对应的 Python 备用命令

建议优先追加到：

- `常用操作命令速查`
- `常见问题`
- `另一台电脑怎么重新搭建`

---

## 12. 常见问题

### 12.1 `torch.cuda.is_available()` 是 `False`

先检查：

```bash
nvidia-smi
```

再检查当前解释器是不是：

```bash
which python
```

然后检查 torch 是不是 CUDA 版：

```bash
python -c "import torch; print(torch.version.cuda)"
```

如果输出 `None`，说明当前装的是 CPU 版 torch。

### 12.2 VS Code 一直提示 `.venv` 缺 pip

处理重点不是修 `.venv`，而是把解释器切到：

```text
/home/josan/miniforge3/envs/labvla-cu124/bin/python
```

### 12.3 下载大包时断掉

优先重试这些：

- 换网络
- 重新执行 pip 安装
- 避开不稳定代理
- 保持在同一个环境里安装，不要混装

### 12.4 运行前不确定自己在哪个环境

直接运行：

```bash
which python
python -c "import sys; print(sys.executable)"
```

只要不是 `labvla-cu124` 的解释器，就先别继续运行项目。

---

## 13. 当前维护结论

截至目前，这个项目在本机最稳定的路线是：

1. `Miniforge + conda` 管理环境
2. `labvla-cu124` 作为主环境
3. `pip + cu124 wheel` 安装 PyTorch
4. VS Code 固定使用 `labvla-cu124` 解释器
5. 模型目录优先复制，环境优先重建
